
## 🎯 **Цель проекта**

Создать **клиент–серверное приложение** на C# (.NET 8), в котором клиенты и поставщики взаимодействуют через сеть:
переписываются, выставляют рейтинги и формируют корзину товаров на основе собственных оценок поставщиков.

Сервер хранит данные в базе (SQLite), обрабатывает несколько клиентов одновременно и предоставляет API через `HttpListener`.
Клиент — это программа, которая подключается к серверу по HTTP, обменивается данными в формате JSON и отображает информацию пользователю.

В будущем клиентскую часть можно без изменений логики перенести в WinForms, т.к. сетевое взаимодействие и бизнес-логика будут изолированы.

---

## 🧩 **Основные участники системы**

1. **Клиент (Client)** — пользователь, который:

   * просматривает список доступных поставщиков и их товары;
   * общается с поставщиками (переписка);
   * ставит каждому поставщику рейтинг (1–10);
   * формирует корзину, исходя из своих личных оценок поставщиков.

2. **Поставщик (Supplier)** — пользователь, который:

   * добавляет и редактирует свои товары;
   * управляет количеством и ценой деталей;
   * общается с клиентами;
   * получает оценки от клиентов.

3. **Сервер** — посредник, который:

   * хранит все данные (клиенты, поставщики, товары, сообщения, оценки);
   * обрабатывает HTTP-запросы от клиентов и поставщиков;
   * собирает корзину на основе заданной логики;
   * поддерживает одновременную работу нескольких клиентов.

4. **База данных (SQLite)** — хранит все сущности.
   Подключение реализовано через слой **Repository**, который изолирует работу с БД от бизнес-логики.
   Благодаря этому можно заменить SQLite на любую другую СУБД без изменения остального кода.

---

## 🧱 **Ключевые сущности**

* **Client** — покупатель с логином и паролем.
* **Supplier** — продавец деталей, также с логином и паролем.
* **Part** — товар (деталь), привязан к поставщику и категории.
* **PartCategory** — категория товаров (например, “Фильтры”, “Свечи”, “Тормозные колодки”).
* **Message** — сообщение между клиентом и поставщиком, содержит отправителя, получателя, текст и время.
* **Rating** — оценка, которую клиент ставит конкретному поставщику.
* **BasketItem** — элемент корзины клиента (поставщик, деталь, количество).
* **Role** — роль пользователя (`Client` или `Supplier`).

---

## 💬 **Функциональные возможности**

### Для клиента:

* Авторизация под своим логином и паролем.
* Просмотр списка поставщиков и их средних оценок.
* Просмотр доступных категорий деталей.
* Переписка с поставщиками (в обе стороны).
* Оценка поставщиков (от 1 до 10).
* Формирование корзины по категории деталей:

  * Клиент выбирает категорию и количество.
  * Система собирает корзину из поставщиков **в порядке убывания его личных оценок**, пока не наберётся нужное количество деталей.

### Для поставщика:

* Авторизация.
* Добавление новых деталей.
* Изменение количества и цены.
* Просмотр переписок с клиентами.
* Просмотр своих рейтингов (средних и от конкретных клиентов).

### Для сервера:

* Поддержка многопоточности (несколько клиентов одновременно).
* REST-подобные HTTP эндпоинты.
* Работа через `HttpListener` (без ASP.NET).
* Хранение данных через слой репозиториев.
* Возможность замены БД без изменения логики.
* Обработка JSON-запросов и ответов.
* Алгоритм сборки корзины по персональным рейтингам.

---

## ⭐ **Ключевая бизнес-логика: рейтинги и корзина**

1. Каждый клиент может поставить каждому поставщику **оценку (1–10)**.
   Если оценка уже есть — она обновляется.

2. У поставщика есть два вида рейтингов:

   * **Средний рейтинг** — среднее значение всех оценок (используется для отображения общего уровня).
   * **Личный рейтинг клиента** — индивидуальная оценка конкретного клиента (используется при сборке корзины).

3. Корзина клиента формируется **только** на основе его личных оценок:

   * Сначала берутся поставщики с самыми высокими оценками.
   * У них выбираются товары нужной категории.
   * Если клиенту нужно больше товаров, чем у поставщика в наличии — добирается у следующего по рейтингу.
   * Если клиент не оценивал поставщика — он не участвует в подборе (или имеет нулевой приоритет).

📘 Пример:

| Поставщик | Деталей в наличии | Оценка от клиента |
| --------- | ----------------- | ----------------- |
| Supplier1 | 3                 | 7                 |
| Supplier2 | 1                 | 10                |
| Supplier3 | 2                 | 8                 |

Если клиенту нужно 4 детали →
1 от Supplier2, 2 от Supplier3, 1 от Supplier1.

---

## ⚙️ **Архитектурные принципы**

* Разделение приложения на **четыре слоя**:

  1. **SharedModels** — модели данных и перечисления (общие для клиента и сервера).
  2. **DataAccess** — работа с БД через репозитории, реализующие интерфейсы.
  3. **Server** — логика обработки запросов и маршрутизация (HttpListener).
  4. **Client** — пользовательское взаимодействие (через консоль, потом WinForms).

* **Паттерн Repository** обеспечивает изоляцию логики доступа к данным от бизнес-логики.
  Сервер не знает, какая БД используется, он просто обращается к интерфейсам.

* **Асинхронность:** все операции (`Task`) — сервер способен обрабатывать несколько запросов одновременно.

* **Гибкость:**

  * БД можно заменить, написав новый класс фабрики (`IRepositoryFactory`).
  * Клиент можно заменить (WinForms, WPF, веб), не трогая сетевую логику.
  * Можно добавить новые типы сущностей, эндпоинтов или расширить протокол.

---

## 🔒 **Авторизация и роли**

* При запуске клиент выбирает роль: **Клиент** или **Поставщик**.
* Входит по логину и паролю.
* Роли определяют, какие функции доступны в интерфейсе.

(Регистрация пока вручную через БД, но архитектура готова для будущего расширения.)

---

## 💻 **Тип взаимодействия**

* Все данные передаются по **HTTP**.
* Формат: **JSON**.
* Клиент использует `HttpClient`, сервер — `HttpListener`.
* Сервер принимает запросы, обрабатывает их через контроллеры и возвращает JSON-ответы.
* Каждое действие — отдельный эндпоинт:
  `/api/messages`, `/api/ratings`, `/api/parts`, `/api/basket`, и т.д.

---

## 🧠 **Смысловая цель проекта**

Создать базу для гибкой, масштабируемой клиент–серверной системы,
которая моделирует взаимодействие покупателей и поставщиков с элементами “социального” взаимодействия (рейтинги, переписка)
и при этом позволяет тестировать бизнес-логику на уровне алгоритмов и сетевого взаимодействия без тяжёлых фреймворков.

---

## 🧩 **Ключевые принципы**

1. Простота и расширяемость.
2. Асинхронность и многопоточность.
3. Изоляция логики данных через репозитории.
4. Возможность масштабирования без изменения базовых компонентов.
5. Реализм в моделировании бизнес-процессов (рейтинги, заказы, переписка).

---

## 🧩 Общая структура решения

```
SupplierClientSolution/
│
├── SharedModels/                ← Общие модели (Entity)
│   ├── BasketItem.cs
│   ├── Client.cs
│   ├── Message.cs
│   ├── Part.cs
│   ├── PartCategory.cs
│   ├── Rating.cs
│   ├── Supplier.cs
│   └── Role.cs
│
├── Database/                    ← Отдельный проект для работы с БД
│   ├── Core/
│   │   ├── IDatabaseInitializer.cs
│   │   ├── IDatabaseConnectionFactory.cs
│   │
│   ├── Sqlite/
│   │   ├── SqliteInitializer.cs
│   │   ├── SqliteConnectionFactory.cs
│   │
│   ├── Repositories/
│   │   ├── Interfaces/
│   │   │   ├── IClientRepository.cs
│   │   │   ├── ISupplierRepository.cs
│   │   │   ├── IMessageRepository.cs
│   │   │   ├── IPartRepository.cs
│   │   │   ├── ICategoryRepository.cs
│   │   │   ├── IRatingRepository.cs
│   │   │   ├── IBasketRepository.cs
│   │   │
│   │   └── Sqlite/
│   │       ├── SqliteClientRepository.cs
│   │       ├── SqliteSupplierRepository.cs
│   │       ├── SqliteMessageRepository.cs
│   │       ├── SqlitePartRepository.cs
│   │       ├── SqliteCategoryRepository.cs
│   │       ├── SqliteRatingRepository.cs
│   │       ├── SqliteBasketRepository.cs
│   │
│   ├── Models/
│   │   └── DatabaseSettings.cs
│   │
│   └── Database.csproj
│
├── Server/                      ← HTTP-сервер
│   ├── Program.cs
│   ├── HttpServer.cs
│   ├── Router.cs
│   │
│   ├── Controllers/
│   │   ├── AuthController.cs
│   │   ├── MessageController.cs
│   │   ├── RatingController.cs
│   │   ├── PartController.cs
│   │   ├── BasketController.cs
│   │
│   ├── Services/
│   │   ├── AuthService.cs
│   │   ├── MessageService.cs
│   │   ├── RatingService.cs
│   │   ├── PartService.cs
│   │   ├── BasketService.cs
│   │
│   ├── Helpers/
│   │   └── ControllerHelper.cs
│   │
│   ├── Dtos/
│   │   ├── AuthDtos.cs
│   │   ├── MessageDtos.cs
│   │   ├── RatingDtos.cs
│   │   ├── PartDtos.cs
│   │   ├── BasketDtos.cs
│   │
│   └── Server.csproj
│
└── Client/                      ← Консольный клиент (позже WinForms)
    ├── Program.cs
    ├── ClientService.cs
    ├── Menu.cs
    └── Client.csproj
```

---

## 🔗 Взаимосвязи проектов

| Проект           | Ссылается на           |
| ---------------- | ---------------------- |
| **SharedModels** | — (базовый проект)     |
| **Database**     | SharedModels           |
| **Server**       | Database, SharedModels |
| **Client**       | SharedModels           |

---

## 📘 Назначение каждого проекта

### 🧱 **SharedModels**

> Общие сущности и перечисления, используемые везде (сервер, клиент, база).

* `Client`, `Supplier`, `Message`, `Part`, `PartCategory`, `Rating`, `BasketItem`
* `Role` — enum с ролями (Client / Supplier)

---

### 💾 **Database**

> Изолированный слой доступа к данным. Можно подменить реализацию SQLite на PostgreSQL, MSSQL и т.д., не меняя сервер.

* Интерфейсы репозиториев (`IClientRepository`, `IMessageRepository` и т.д.)
* Реализация под SQLite (`SqliteClientRepository`, `SqliteMessageRepository` и др.)
* Настройка БД (`SqliteConnectionFactory`, `SqliteInitializer`)
* Использует `Microsoft.Data.Sqlite`

---

### 🌐 **Server**

> Принимает HTTP-запросы через `HttpListener`, маршрутизирует их и вызывает соответствующие контроллеры и сервисы.

* **Controllers** — маршруты и HTTP-логика
* **Services** — бизнес-логика (работа с репозиториями, валидация и т.д.)
* **Helpers** — утилиты для JSON, ошибок и парсинга запросов
* **Dtos** — входные и выходные модели для API
* **Router** — таблица маршрутов (метод + путь → обработчик)
* **HttpServer** — прослушивает и распределяет запросы
* **Program.cs** — связывает всё, внедряет зависимости, запускает сервер

---

### 💬 **Client**

> Простая консольная программа (позже WinForms), работающая через `HttpClient`.

* Отправляет запросы на сервер (`/auth/login`, `/messages`, `/basket`, `/rating` и т.д.)
* Позволяет:

  * Войти как клиент или поставщик
  * Отправлять и читать сообщения
  * Ставить оценки
  * Просматривать и добавлять детали
  * Собирать корзину (на основе рейтингов конкретного клиента)

---

## ⚙️ Ключевая идея

Благодаря **чистому разделению**:

* БД можно заменить, просто написав `PostgresClientRepository` и т.д.
* Сервер не знает, как хранится информация
* Клиент не знает, как работает сервер внутри
* Общие данные (модели) остаются согласованными

---

Хочешь, я теперь выведу **всю иерархию и описание проекта Database** отдельно (чтобы ты видел именно этот слой в деталях)?
Это будет логичный шаг перед интеграцией с сервером.

